/**
 * Entry gulpfile.
 *
 * Gulp tasks declared in the @netzstrategen/gulp-task-collection package.
 * Asset paths set in package.json.
 */

const gulp = require('gulp');
require('@netzstrategen/gulp-task-collection')(gulp);
